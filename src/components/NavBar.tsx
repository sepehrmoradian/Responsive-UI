import styles from "./styles/navBar.module.css";

const NavBar = () => {
  return (
    <nav className={styles.bar}>
      <p className={styles.text}>Registration Form</p>
    </nav>
  );
};

export default NavBar;
